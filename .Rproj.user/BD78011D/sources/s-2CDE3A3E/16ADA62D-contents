---
title: "Untitled"
author: "Lothar Mateo Rojas Sosa"
date: '2022-07-23'
output: 
  pdf_document:
      highlight: zenburn
header-includes:
  - \usepackage{soul}
  - \usepackage{color}
  - \DeclareRobustCommand{\hlcyan}[1]{{\sethlcolor{cyan}\hl{#1}}}
  - \usepackage{xspace}
  - \usepackage{tcolorbox}
  - \tcbuselibrary{listings}
  - \newtcblisting{foo}{ listing only, nobeforeafter, after={\xspace}, hbox, tcbox raise base,fontupper=\ttfamily,colback=lightgray,colframe=lightgray,size=fbox}
  - \usepackage{xcolor}
  - \definecolor{code_gray}{rgb}{0.8,0.8,0.8}
  - \definecolor{bcode}{RGB}{216, 220, 231}
  - \tcbset{on line, boxsep=4pt, left=0pt,right=0pt,top=0pt,bottom=0pt, colframe=white,colback=bcode }


---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE)
```

## R Markdown

This is an R Markdown document. \tcbox{\texttt{ Markdown}} `Markdown` is a \begin{foo} simple formatting \end{foo}  syntax for authoring HTML, PDF, and MS Word documents. For more details on using R Markdown see <http://rmarkdown.rstudio.com>.


When you click the **Knit** \colorbox{code_gray}{`HTML`} button a document will be generated that includes both content as well as the output of any embedded R code chunks within the document. You can embed an R code chunk like this:

```{r cars}
summary(cars)

# This is a comment

x <- 4*99

help <- data.frame( x = 1:20, y = 21:40 )

print("heeell")

function(gj){
  
}
```

## Including Plots

You can also embed plots, for example:

```{r pressure, echo=FALSE}
plot(pressure)
```

Note that the `echo = FALSE` parameter was added to the code chunk to prevent printing of the R code that generated the plot.
